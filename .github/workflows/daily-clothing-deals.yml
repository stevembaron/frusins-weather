name: Update Daily Clothing Deals

on:
  schedule:
    - cron: "15 12 * * *"
    - cron: "45 12 * * *"
    - cron: "15 13 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-clothing-deals
  cancel-in-progress: false

jobs:
  update-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Make publish helper executable
        run: chmod +x scripts/publish_generated_report.sh

      - name: Check if today's report is already published
        id: freshness
        run: |
          should_run=true
          if [ "${{ github.event_name }}" = "schedule" ] && [ -f data/clothing_deals.json ]; then
            report_date="$(python3 -c 'import json; print(json.load(open("data/clothing_deals.json")).get("generated_at", "")[:10])')"
            today="$(date -u +%F)"
            if [ "$report_date" = "$today" ]; then
              should_run=false
              echo "Today's clothing deal report is already published."
            fi
          fi
          echo "should_run=$should_run" >> "$GITHUB_OUTPUT"

      - name: Run clothing deal monitor
        if: steps.freshness.outputs.should_run == 'true'
        run: python3 scripts/deal_monitor.py --config config/clothing_deal_sources.json

      - name: Publish updated report
        if: steps.freshness.outputs.should_run == 'true'
        run: >
          scripts/publish_generated_report.sh
          "Update daily clothing deals"
          data/clothing_deal_report.md
          data/clothing_deals.html
          data/clothing_deals.json
          clothing-deals/index.html
